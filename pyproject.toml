[tool.ruff]
line-length = 88
target-version = ["py310"]
exclude = [".git", "__pycache__", "build", "dist"]

[tool.ruff.per-file-ignores]
# allow tests to use shorter variable names and pytest styles
"tests/*" = ["D", "S101"]
